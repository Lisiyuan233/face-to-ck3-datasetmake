# CK3 图像与 DNA 受校验采集

采集入口为 `face_to_ck3_tool.py`。新版流程把每个样本作为一个事务处理，只有图片和 DNA
通过同步校验后才会占用 sample ID。

## 建议启动方式

重新采集时使用新目录，不要直接混入旧的 510,000 样本：

```powershell
python face_to_ck3_tool.py --base-dir face_to_ck3_dataset_male_v2
```

先采集 10～20 个样本，人工确认图片、DNA 和编号，再开始长时间任务。

## 单样本流程

1. 点击“随机生成外貌”，随后把鼠标移到截图区域中央，防止按钮 tooltip 固定。
2. 使用剪贴板 sentinel 点击“复制 DNA”；点击未生效时，旧剪贴板内容不能通过校验。
3. 检查 DNA 与上一条不同。
4. 间隔一段时间再次复制；连续两次解析结果一致，才认为 CK3 状态稳定。
5. 截图。
6. 截图后再次复制 DNA；必须与截图前完全一致。
7. 先写 `.partial` 文件，再以 DNA、图片的顺序原子提交，图片最后提交。

## 自动切换种族

点击主界面的“设置种族切换”，依次标定种族按钮、种族列表第一项和第二项、
“显示头发与胡须”复选框及“面部结构”按钮。标定保存后会自动启用此功能。

默认每个种族连续采集 30,000 个样本。进入下一个 30,000 样本块前，脚本按顺序：

1. 点击种族按钮；
2. 选择目标种族（由样本编号计算，不盲目点击相对“下一项”）；
3. 点击“显示头发与胡须”，取消勾选；
4. 点击“面部结构”，恢复采集界面。

列表第一项和第二项用于计算固定行距，因此一次标定即可选择后续各项。默认种族总数为
17；“校验设置”中可以调整每个种族的样本数、种族总数，或暂时关闭自动切换。
自动切换只在块边界执行，例如 `face_30001`、`face_60001` 之前；从块中间恢复采集时
不会重复切换。

主界面的“本次采集样本数”只控制这次运行要成功保存多少份样本；“每个种族样本数”
控制种族块大小。点击“开始运行”时两个值会一起校验并保存。采集已经开始后再修改设置
不会改变正在运行的工作线程，需要安全取消后重新开始，以免一个种族块内混用两套边界。

任意一步失败都会丢弃本次内存截图并重新随机生成，不写 PNG/TXT，也不增加 sample ID。

## 恢复与安全检查

- 启动时检查 `face/face_N.png` 与 `dna/face_N.txt` 是否一一对应。
- 存在孤立图片或 DNA 时拒绝继续，避免编号错位。
- 从最大完整编号的下一条继续，并将最后一份 DNA 作为重复检测基线。
- `collection_progress.json` 记录最近成功提交的样本及 DNA 指纹。
- PyAutoGUI 左上角 failsafe 和进度窗口“取消”都会安全停止；未完成事务不会落盘。

## 默认校验参数

| 参数 | 默认值 |
|---|---:|
| 随机生成后初始等待 | 1.5 秒 |
| DNA 稳定复核间隔 | 0.5 秒 |
| 单次稳定等待超时 | 8 秒 |
| 随机生成最大尝试 | 4 次 |
| 样本事务额外重试 | 2 次 |
| DNA 稳定后截图等待 | 0.5 秒 |

## 设置持久化

界面会把截图区域、复制 DNA/随机生成按钮坐标、种族切换标定、循环次数和全部校验参数保存到数据集
目录下的 `collection_settings.json`。再次使用同一个 `--base-dir` 启动时会自动恢复；
不同数据集目录可以保留各自独立的屏幕布局和采集参数。

设置文件使用临时文件原子替换。文件损坏、版本不兼容或参数未通过安全校验时，程序会
保留原文件、回退到默认值并在启动界面提示，不会带着无效参数开始采集。

可以从界面的“校验设置”调整。CK3 帧率较低、游戏在机械硬盘上或后台负载较高时，应增加
随机生成后等待与稳定等待超时，不应缩短到旧流程使用的 0.2 秒。

## 采集后抽查

每完成一个小批次，至少检查：

- 相邻 DNA 指纹不重复；
- 随机抽取图片，将其 DNA 重新导入 CK3 后稳定轮廓一致；
- `face` 和 `dna` 文件数量及编号集合完全相同；
- 没有残留 `.partial` 文件。
