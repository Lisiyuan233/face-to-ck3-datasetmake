<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <include ignore_missing="yes">/etc/fonts/fonts.conf</include>
  <!-- WSL can render the fonts already installed by Windows. -->
  <dir>/mnt/c/Windows/Fonts</dir>
</fontconfig>
